spring:
  config:
    activate:
      on-profile: prod
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

logging:
  level:
    com.hotel.operacloud: INFO
    org.springframework.kafka: WARN
    org.hibernate.SQL: WARN
    liquibase: WARN
